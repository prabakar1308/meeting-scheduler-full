# Agentic Meeting Scheduler - Full Scaffold (LangGraph Agent inside backend)

This scaffold includes:
- backend: NestJS with an embedded LangChain + LangGraph agent in `backend/src/agent`
- agent uses LangChain/ LangGraph (OpenAI / Anthropic support via env)
- frontend: minimal Next.js placeholder
- Prisma schema (SQLite)

See `backend/README.md` for run instructions.
